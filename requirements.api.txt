# API/server extras (FastAPI stack)
# Includes core runtime via -r reference

-r requirements.core.txt

fastapi==0.121.1
# Align with FastAPI constraint: <0.50.0 and >=0.40.0
starlette==0.49.0
pydantic==2.12.4
uvicorn==0.38.0
httpx==0.28.1
