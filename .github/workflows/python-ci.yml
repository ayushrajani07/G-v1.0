name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run tests (quick)
        run: |
          pytest -q --disable-warnings --maxfail=1

      - name: Coverage (xml)
        run: |
          pytest -q --disable-warnings --cov=. --cov-report=xml

      - name: Validate Prometheus alert rules (drift)
        run: |
          set -e
          VERSION=v2.52.0
          curl -s -L https://github.com/prometheus/prometheus/releases/download/${VERSION}/prometheus-2.52.0.linux-amd64.tar.gz -o promtool.tgz
          tar -xzf promtool.tgz
          ./prometheus-2.52.0.linux-amd64/promtool check rules prometheus_alerts_drift.yml
          echo "Prometheus alert rules validation succeeded"

      - name: Upload coverage xml
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.python-version }}
          path: coverage.xml

  lint:
    name: Lint (non-blocking)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install linters
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Ruff
        run: |
          ruff check . --format=github --exit-zero

      - name: Mypy (path_forecast only)
        run: |
          mypy src/path_forecast || true
