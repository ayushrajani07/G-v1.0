apiVersion: 1
groups:
  - orgId: 1
    name: G6 ML Alerts
    folder: G6 ML
    interval: 1m
    rules:
      # Rules will be loaded from files in provisioning/alerting directory by Grafana automatically if referenced.
